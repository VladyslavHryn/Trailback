# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Kyiv*Type source release (315 files: OTF/TTF/WEB + variable GX), kept out of
# the repo. The face is not in use — see the note in src/index.css for why it
# was tried and reverted — but the release stays ignored so dropping it back in
# here never accidentally commits 315 font files.
KyivType-NoVariable/
KyivType-VariableGX/

# Local secrets. .env.example is the committed template.
.env
.env.local

# Wrangler
.wrangler/
